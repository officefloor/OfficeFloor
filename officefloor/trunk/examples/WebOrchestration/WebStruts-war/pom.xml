<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>net.officefloor.example.weborchestration</groupId>
		<artifactId>WebOrchestration</artifactId>
		<version>0.0.1</version>
	</parent>
	<artifactId>WebStruts-war</artifactId>
	<version>0.0.1</version>
	<packaging>war</packaging>
	<name>WebStruts-war</name>
	<description>Web components of the Web Struts Example</description>
	<build>
		<plugins>
			<plugin>
				<!-- Run Jetty for integration tests -->
				<groupId>org.mortbay.jetty</groupId>
				<artifactId>maven-jetty-plugin</artifactId>
				<dependencies>
					<!-- To use OpenEJB, must exclude ANT from jsp-2.1-jetty -->
					<dependency>
						<groupId>org.mortbay.jetty</groupId>
						<artifactId>jsp-2.1-jetty</artifactId>
						<version>6.1.26</version>
						<exclusions>
							<exclusion>
								<groupId>ant</groupId>
								<artifactId>ant</artifactId>
							</exclusion>
						</exclusions>
					</dependency>
					<!-- Use OpenEJB to incorporate EJBs for orchestration -->
					<dependency>
						<groupId>org.apache.openejb</groupId>
						<artifactId>openejb-core</artifactId>
						<version>3.1.3</version>
					</dependency>
					<dependency>
						<groupId>${project.groupId}</groupId>
						<artifactId>WebOrchestration-ejb</artifactId>
						<version>${project.version}</version>
					</dependency>
				</dependencies>
				<configuration>
					<jettyConfig>${basedir}/src/main/jetty/jetty.xml</jettyConfig>
					<scanIntervalSeconds>10</scanIntervalSeconds>
					<stopPort>8005</stopPort>
					<stopKey>STOP</stopKey>
					<contextPath>/</contextPath>
					<systemProperties>
						<!-- Properties to use OpenEJB -->
						<systemProperty>
							<name>java.naming.factory.initial</name>
							<value>org.apache.openejb.client.LocalInitialContextFactory</value>
						</systemProperty>
						<systemProperty>
							<name>openejb.deployments.classpath.include</name>
							<value>.*WebOrchestration.*</value>
						</systemProperty>
					</systemProperties>
				</configuration>
				<executions>
					<execution>
						<id>start-jetty</id>
						<phase>pre-integration-test</phase>
						<goals>
							<goal>run</goal>
						</goals>
						<configuration>
							<scanIntervalSeconds>0</scanIntervalSeconds>
							<daemon>true</daemon>
						</configuration>
					</execution>
					<execution>
						<id>stop-jetty</id>
						<phase>post-integration-test</phase>
						<goals>
							<goal>stop</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
	<dependencies>
		<dependency>
			<groupId>${project.groupId}</groupId>
			<artifactId>WebOrchestration-ejb</artifactId>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>org.apache.struts</groupId>
			<artifactId>struts2-core</artifactId>
		</dependency>
		<dependency>
			<groupId>${project.groupId}</groupId>
			<artifactId>WebTests</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>
</project>