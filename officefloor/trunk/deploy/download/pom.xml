<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>net.officefloor.deploy</groupId>
		<artifactId>deploy</artifactId>
		<version>0.1.2</version>
	</parent>
	<artifactId>officefloor</artifactId>
	<name>OfficeFloor Download Deployment</name>
	<description>Generates and does the deployment of the OfficeFloor download release</description>
	<build>
		<plugins>
			<plugin>
				<!-- Generates the release -->
				<artifactId>maven-assembly-plugin</artifactId>
				<executions>
					<execution>
						<id>release</id>
						<phase>package</phase>
						<goals>
							<goal>single</goal>
						</goals>
						<configuration>
							<descriptors>
								<descriptor>release-assembly.xml</descriptor>
							</descriptors>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<artifactId>maven-antrun-plugin</artifactId>
				<executions>
					<execution>
						<id>deploy-release</id>
						<phase>deploy</phase>
						<goals>
							<goal>run</goal>
						</goals>
						<configuration>
							<tasks>
								<echo>Deploying the Download release to SourceForge</echo>
								<property name="deploy.content.dir" value="${project.build.directory}/sourceforge_deploy_content" />
								<mkdir dir="${deploy.content.dir}/${project.version}" />
								<copy file="${project.build.directory}/${project.artifactId}-${project.version}-release.zip" todir="${deploy.content.dir}/${project.version}" />
								<exec dir="${deploy.content.dir}" executable="rsync" failonerror="true">
									<arg line="-e ssh -r . sagenschneider,officefloor@frs.sourceforge.net:/home/frs/project/o/of/officefloor/OfficeFloor/" />
								</exec>
							</tasks>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
	<distributionManagement>
		<repository>
			<id>Ignore</id>
			<name>Ignore as using ant to deploy</name>
			<url>file://${java.io.tmpdir}</url>
		</repository>
	</distributionManagement>
	<dependencies>
		<dependency>
			<groupId>net.officefloor.core</groupId>
			<artifactId>latest</artifactId>
			<version>SNAPSHOT</version>
		</dependency>
		<dependency>
			<groupId>net.officefloor.plugin</groupId>
			<artifactId>latest</artifactId>
			<version>SNAPSHOT</version>
		</dependency>
		<dependency>
			<groupId>net.officefloor.tool</groupId>
			<artifactId>latest</artifactId>
			<version>SNAPSHOT</version>
		</dependency>
	</dependencies>
</project>