<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>net.officefloor.eclipse</groupId>
		<artifactId>eclipse</artifactId>
		<version>0.2.1</version>
	</parent>
	<artifactId>update.site</artifactId>
	<version>0.2.1</version>
	<name>Eclipse Update Site</name>
	<description>
		Generates update site content for OfficeFloor in Eclipse.
	</description>
	<packaging>pom</packaging>
	<properties>
		<update.site.package.dir>${project.build.directory}/update_site_package_dir</update.site.package.dir>
		<update.site.deploy.dir>${project.build.directory}/update_site_deploy_dir</update.site.deploy.dir>
	</properties>
	<build>
		<plugins>
			<plugin>
				<!-- Work around for eclipse:eclipse for this pom project. -->
				<artifactId>maven-eclipse-plugin</artifactId>
				<configuration>
					<packaging>jar</packaging>
				</configuration>
			</plugin>
			<plugin>
				<artifactId>maven-dependency-plugin</artifactId>
				<executions>
					<execution>
						<id>copy-update-site-content</id>
						<phase>package</phase>
						<goals>
							<goal>copy-dependencies</goal>
						</goals>
						<configuration>
							<outputDirectory>${update.site.package.dir}
							</outputDirectory>
							<overWriteIfNewer>true</overWriteIfNewer>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<artifactId>maven-antrun-plugin</artifactId>
				<executions>
					<execution>
						<id>build-update-site-content</id>
						<phase>package</phase>
						<goals>
							<goal>run</goal>
						</goals>
						<configuration>
							<tasks>
								<!-- Make the ant-contrib tasks available -->
								<taskdef resource="net/sf/antcontrib/antlib.xml"
									classpathref="maven.dependency.classpath" />

								<!-- Obtain the artifact download cache directory -->
								<property name="DOWNLOAD_ARTIFACT_CACHE_DIR"
									value="${settings.localRepository}/officefloor_updatesite_cache" />
								<mkdir dir="${DOWNLOAD_ARTIFACT_CACHE_DIR}" />

								<!-- Build the update site content -->
								<ant dir="${project.basedir}" antfile="build-update-site-content.xml"
									target="build-update-site-content">
									<property name="BUILD_DIR" value="${update.site.package.dir}" />
									<property name="P2_LAUNCH_JAR"
										value="${eclipse.install.dir}/plugins/org.eclipse.equinox.launcher_${equinox.version}.jar" />
									<property name="OFFICE_FLOOR_VERSION" value="${project.version}" />
									<property name="DOWNLOAD_ARTIFACT_CACHE_DIR" value="${DOWNLOAD_ARTIFACT_CACHE_DIR}" />
								</ant>
							</tasks>
						</configuration>
					</execution>
					<execution>
						<id>deploy-update-site-content</id>
						<phase>deploy</phase>
						<goals>
							<goal>run</goal>
						</goals>
						<configuration>
							<tasks>
								<!--
									Collect only artifacts to deploy. Stops overwriting (and
									potentially corrupting) existing released artifacts.
								-->
								<echo>Collecting update site content to deploy</echo>
								<mkdir dir="${update.site.deploy.dir}" />
								<copy todir="${update.site.deploy.dir}" overwrite="true">
									<fileset dir="${update.site.package.dir}">
										<!-- Reference files -->
										<include name="index.html" />
										<include name="site.xml" />
										<include name="artifacts.jar" />
										<include name="content.jar" />
										<include name="digest.zip" />

										<!-- Core feature and its Eclipse plug-ins -->
										<include name="features/net.officefloor.feature_${pom.version}.jar" />
										<include name="plugins/net.officefloor.core_${pom.version}.jar" />
										<include
											name="plugins/net.officefloor.core_${pom.version}.jar.pack.gz" />
										<include name="plugins/net.officefloor.ui_${pom.version}.jar" />
										<include
											name="plugins/net.officefloor.ui_${pom.version}.jar.pack.gz" />

										<!-- Plug-ins feature and its Eclipse plug-ins -->
										<include
											name="features/net.officefloor.plugins.feature_${pom.version}.jar" />
										<include name="plugins/net.officefloor.jdbc_${pom.version}.jar" />
										<include
											name="plugins/net.officefloor.jdbc_${pom.version}.jar.pack.gz" />
										<include name="plugins/net.officefloor.jndi_${pom.version}.jar" />
										<include
											name="plugins/net.officefloor.jndi_${pom.version}.jar.pack.gz" />
										<include name="plugins/net.officefloor.socket_${pom.version}.jar" />
										<include
											name="plugins/net.officefloor.socket_${pom.version}.jar.pack.gz" />
										<include
											name="plugins/net.officefloor.filingcabinet_${pom.version}.jar" />
										<include
											name="plugins/net.officefloor.filingcabinet_${pom.version}.jar.pack.gz" />
										<include name="plugins/net.officefloor.demo_${pom.version}.jar" />
										<include
											name="plugins/net.officefloor.demo_${pom.version}.jar.pack.gz" />
									</fileset>
								</copy>

								<echo>Deploying the update site content to SourceForge</echo>
								<exec dir="${update.site.deploy.dir}" executable="rsync"
									failonerror="true">
									<arg
										line="-e ssh -r . sagenschneider,officefloor@frs.sourceforge.net:/home/frs/project/o/of/officefloor/eclipse/update" />
								</exec>
							</tasks>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<artifactId>maven-assembly-plugin</artifactId>
				<executions>
					<execution>
						<id>update-site-zip</id>
						<phase>package</phase>
						<goals>
							<goal>single</goal>
						</goals>
						<configuration>
							<descriptors>
								<descriptor>update-site-assembly.xml</descriptor>
							</descriptors>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
	<dependencies>
		<dependency>
			<groupId>net.officefloor.eclipse</groupId>
			<artifactId>net.officefloor.feature</artifactId>
		</dependency>
		<dependency>
			<groupId>net.officefloor.eclipse</groupId>
			<artifactId>net.officefloor.plugins.feature</artifactId>
		</dependency>
		<dependency>
			<groupId>ant-contrib</groupId>
			<artifactId>ant-contrib</artifactId>
			<version>1.0b3</version>
		</dependency>
	</dependencies>
	<distributionManagement>
		<repository>
			<id>Ignore</id>
			<name>Ignore as using ant to deploy</name>
			<url>file://${java.io.tmpdir}</url>
		</repository>
	</distributionManagement>
</project>