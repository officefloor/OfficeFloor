<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>net.officefloor</groupId>
		<artifactId>officefloor</artifactId>
		<version>0.0.1</version>
	</parent>
	<artifactId>net.officefloor.update.site
	</artifactId>
	<name>Office Floor Eclipse Update Site</name>
	<description>
		Generates update site content for Office Floor Eclipse.
	</description>
	<packaging>pom</packaging>
	<build>
		<plugins>
			<plugin>
				<!-- Work around for eclipse:eclipse for this pom project. -->
				<artifactId>maven-eclipse-plugin</artifactId>
				<configuration>
					<packaging>jar</packaging>
				</configuration>
			</plugin>
			<plugin>
				<!-- Ensure the features are copied locally to create update site -->
				<artifactId>maven-dependency-plugin
				</artifactId>
				<executions>
					<execution>
						<id>copy-dependencies</id>
						<phase>process-sources</phase>
						<goals>
							<goal>copy-dependencies</goal>
						</goals>
						<configuration>
							<outputDirectory>
								${build.directory}/dependencies
							</outputDirectory>
							<overWriteReleases>false</overWriteReleases>
							<overWriteSnapshots>false</overWriteSnapshots>
							<overWriteIfNewer>true</overWriteIfNewer>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<!-- Use ant to extract and build the eclipse update site content -->
				<artifactId>maven-antrun-plugin</artifactId>
				<executions>
					<execution>
						<id>compile</id>
						<phase>compile</phase>
						<goals>
							<goal>run</goal>
						</goals>
						<configuration>
							<tasks>
								<ant antfile="${basedir}/generateUpdateSiteBuild.xml" dir="${basedir}">
									<property name="build.dir" value="${project.build.directory}" />
									<property name="dependency.dir"
										value="${project.build.directory}/dependencies" />
									<property name="update.site.dir" value="${project.build.directory}/updatesite" />
									<property name="version" value="${pom.version}" />
								</ant>
							</tasks>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<!-- Assemble the update site -->
				<artifactId>maven-assembly-plugin</artifactId>
				<configuration>
					<descriptors>
						<descriptor>assemble.xml</descriptor>
					</descriptors>
				</configuration>
				<executions>
					<execution>
						<id>make-assembly</id>
						<phase>package</phase>
						<goals>
							<goal>single</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
	<dependencies>
		<dependency>
			<groupId>${pom.groupId}</groupId>
			<artifactId>net.officefloor.feature
			</artifactId>
			<version>${pom.version}</version>
			<type>zip</type>
		</dependency>
		<dependency>
			<groupId>${pom.groupId}</groupId>
			<artifactId>net.officefloor.plugins.feature
			</artifactId>
			<version>${pom.version}</version>
			<type>zip</type>
		</dependency>
	</dependencies>
</project>