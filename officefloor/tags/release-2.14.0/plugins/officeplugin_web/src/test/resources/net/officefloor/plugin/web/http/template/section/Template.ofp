<html>
<body>
<p>Template ${TemplateName}</p>
<p>${EscapedHtml} ${UnescapedHtml}</p>
<p>${nullBean not to be displayed $} <!-- ${bean --> Bean with property ${beanProperty} <!-- $} --> ${beanArray ${count}  $}</p>
<table>
    <tr>
        <td>Name</td>
        <td>Description</td>
    </tr>
    <!-- {List} -->
    <tr>
        <td>${Name}</td>
        <td>${description}</td>
    </tr>
    <!-- {Tail} -->
</table>
<form action="#{nextTask}">
	<input type="submit" />
</form>
<form action="#{submit}">
    <input type="submit" />
</form>
<a href="#{nonMethodLink}">Non-method link</a>
<a href="#{notRenderTemplateAfter}">Not render template after link</a>
</body>
</html>