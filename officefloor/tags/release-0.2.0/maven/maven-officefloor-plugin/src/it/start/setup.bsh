import java.io.*;

// Log setting up
System.out.println("Setting up project run");

// Ensure file removed
File file = new File(basedir, "target/OfficeFloorRun.txt");
if ( file.isFile() ) {
	file.delete();
}