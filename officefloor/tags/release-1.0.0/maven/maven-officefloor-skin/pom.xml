<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>net.officefloor.maven</groupId>
		<artifactId>maven</artifactId>
		<version>1.0.0</version>
	</parent>
	<artifactId>maven-officefloor-skin</artifactId>
	<version>1.0.0</version>
	<name>Site Skin</name>
	<description>Skin for OfficeFloor web site</description>
	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
	</properties>
	<build>
		<plugins>
			<plugin>
				<!-- Obtain the Syntax Highlighter files -->
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-antrun-plugin</artifactId>
				<executions>
					<execution>
						<id>download-syntax-highlighter-files</id>
						<phase>process-sources</phase>
						<goals>
							<goal>run</goal>
						</goals>
						<configuration>
							<target>
								<!-- Setup directories for download -->
								<property name="target.dir" value="${project.build.outputDirectory}" />
								<property name="script.dir" value="${target.dir}/scripts" />
								<property name="css.dir" value="${target.dir}/css" />
								<mkdir dir="${script.dir}" />
								<mkdir dir="${css.dir}" />

								<!-- Setup URLs for download -->
								<property name="highlight.url"
									value="http://alexgorbatchev.com/pub/sh/current" />
								<property name="script.url" value="${highlight.url}/scripts" />
								<property name="css.url" value="${highlight.url}/styles" />

								<!-- Download the Syntax Highlighter files -->
								<echo>Download Syntax Highlighter files:
									${project.build.outputDirectory}</echo>
								<get dest="${script.dir}" verbose="true" skipexisting="true">
									<url url="${script.url}/shCore.js" />
									<url url="${script.url}/shAutoloader.js" />
									<url url="${script.url}/shBrushJava.js" />
									<url url="${script.url}/shBrushXml.js" />
									<url url="${script.url}/shBrushPlain.js" />
								</get>
								<get dest="${css.dir}" verbose="true" skipexisting="true">
									<url url="${css.url}/shCore.css" />
									<url url="${css.url}/shThemeDefault.css" />
								</get>
							</target>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>