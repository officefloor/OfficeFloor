<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>net.officefloor.core</groupId>
		<artifactId>core</artifactId>
		<version>3.0.0</version>
	</parent>
	<artifactId>officecompiler</artifactId>
	<name>OfficeCompiler</name>
	<description>Compiler for OfficeFloor (compiles the sources into the frame for execution)</description>
	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
	</properties>
	<build>
		<plugins>
			<plugin>
				<!-- Generate model source files -->
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-antrun-plugin</artifactId>
				<executions>
					<execution>
						<id>generate-model</id>
						<phase>generate-sources</phase>
						<goals>
							<goal>run</goal>
						</goals>
						<configuration>
							<target>
								<!-- Specifying properties -->
								<property name="modelprojectdir" value="${basedir}" />
								<property name="compile_classpath" refid="maven.compile.classpath" />
								<!-- Generating the model classes -->
								<java classname="net.officefloor.model.generate.GenerateOfficeFloorModel">
									<arg value="${modelprojectdir}/src/raw/model" />
									<arg value="${modelprojectdir}/src/auto/model" />
									<classpath>
										<pathelement path="${compile_classpath}" />
									</classpath>
								</java>
							</target>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<!-- Include additional source and resource directories -->
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>build-helper-maven-plugin</artifactId>
				<executions>
					<execution>
						<id>add-source</id>
						<phase>generate-sources</phase>
						<goals>
							<goal>add-source</goal>
						</goals>
						<configuration>
							<sources>
								<source>src/main/impl</source>
								<source>src/auto/model</source>
								<source>src/main/plugin</source>
								<source>src/main/test</source>
							</sources>
						</configuration>
					</execution>
					<execution>
						<id>add-resource</id>
						<phase>generate-sources</phase>
						<goals>
							<goal>add-resource</goal>
						</goals>
						<configuration>
							<resources>
								<resource>
									<directory>src/raw/model</directory>
								</resource>
							</resources>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<!-- Additionally clean the model source files -->
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-clean-plugin</artifactId>
				<configuration>
					<filesets>
						<fileset>
							<directory>${basedir}</directory>
							<includes>
								<include>src/auto/model/net/</include>
								<include>bin/</include>
							</includes>
						</fileset>
					</filesets>
				</configuration>
			</plugin>
		</plugins>
	</build>
	<dependencies>
		<dependency>
			<groupId>${project.groupId}</groupId>
			<artifactId>officeframe</artifactId>
		</dependency>
		<dependency>
			<groupId>${project.groupId}</groupId>
			<artifactId>officexml</artifactId>
		</dependency>
		<dependency>
			<groupId>${project.groupId}</groupId>
			<artifactId>officemodelgen</artifactId>
		</dependency>
	</dependencies>
	<profiles>
		<profile>
			<!-- Avoid SourceForge Shell timeout by recreating the shell -->
			<id>shutdown-and-create-sourceforge-shell</id>
			<activation>
				<property>
					<name>deploy</name>
				</property>
			</activation>
			<build>
				<plugins>
					<plugin>
						<!-- Shutdown and Create SourceForge Shell (avoids timeout) -->
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-antrun-plugin</artifactId>
						<executions>
							<execution>
								<id>Shutdown and Create SourceForge shell</id>
								<inherited>false</inherited>
								<phase>verify</phase>
								<goals>
									<goal>run</goal>
								</goals>
								<configuration>
									<target>
										<!-- Shutdown the SourceForge shell -->
										<sshexec host="shell.sourceforge.net" username="${developer.svn.username},officefloor"
											password="${developer.svn.password}" verbose="true" command="shutdown" />

										<!-- Wait 2 minutes to allow complete shutdown -->
										<echo>Waiting 2 minutes to allow SourceForge shell shutdown</echo>
										<sleep minutes="2" />

										<!-- Create new SourceForge shell -->
										<sshexec host="shell.sourceforge.net" username="${developer.svn.username},officefloor"
											password="${developer.svn.password}" verbose="true" command="create" />
									</target>
								</configuration>
							</execution>
						</executions>
						<dependencies>
							<dependency>
								<groupId>org.apache.ant</groupId>
								<artifactId>ant-jsch</artifactId>
								<version>${ant-jsch-version}</version>
								<scope>runtime</scope>
							</dependency>
						</dependencies>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>